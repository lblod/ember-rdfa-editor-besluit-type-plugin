<PowerSelect
  @disabled={{@disabled}}
  {{!--
    disabled search for now as it's broken in the GN modal
                       --}}
   {{!--
     @placeholder="Jaarrekening, ..."
     @search={{this.search}}
     @searchEnabled={{true}}
             --}}
  @searchField="label"
  @options={{this.besluitTypes}}
  @selected={{@selected}}
  @onChange={{@onchange}} as |besluitType|>
  {{besluitType.label}}
</PowerSelect>
