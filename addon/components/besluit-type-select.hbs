<PowerSelect @disabled={{@disabled}} @placeholder="Besluitenlijst, jaarrekening, ..." @search={{this.search}} @searchPlaceholder="Typ om te zoeken..." @searchEnabled={{true}} @options={{@besluitTypes}} @selected={{@selected}} @onchange={{@onchange}} as |besluitType|>
  {{besluitType.label}}
</PowerSelect>