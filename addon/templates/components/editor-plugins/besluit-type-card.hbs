<p class="u-spacer--tiny">{{info.label}} <br>
  <span>Decision Type: </span>
  <BesluitTypeSelect @besluitTypes={{besluitTypes}} @onchange={{action "updateBesluitType"}} @selected={{this.besluitType}} />
</p>

<WuButtonGroup>
  <WuButton @label="Voeg in" class="button button--narrow" @onClick={{action 'insert'}} />
</WuButtonGroup>

{{yield}}
